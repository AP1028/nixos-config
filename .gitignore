firmware/
result
.claw/
# local.nix holds personal values (username, description, config dir); it lives
# only on this machine and must never be committed. Tracked template:
# local.nix.template (rebuild.sh copies it on first run).
local.nix
git-excluded/
__pycache__/

# ZCode session state (plans, etc.) — local only, never commit.
.zcode/
